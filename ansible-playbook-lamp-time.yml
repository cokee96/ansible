---
- name: deploy common configuration
  hosts: common
  remote_user: coke
  become: yes
  roles:
    - lamp-server-time
  vars:
    action: "common"

- name: deploy common configuration
  hosts: webservers
  remote_user: coke
  become: yes
  roles:
    - lamp-server-time
  vars:
    action: "web"

- name: deploy common configuration
  hosts: dbservers
  remote_user: coke
  become: yes
  roles:
    - lamp-server-time
  vars:
    action: "db"